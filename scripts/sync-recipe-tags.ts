/**
 * 同步食谱标签数据 (Stubbed)
 *
 * 此脚本已废弃 - 新架构使用统一的 Tag 模型和 RecipeTag 关联表
 * 旧的分离模型 (Scene, CookingMethod, Taste, Crowd, Occasion) 及其关联表已被移除
 */

console.log("⚠️ 此脚本已废弃");
console.log("新架构使用统一的 Tag 模型 (type: scene|method|taste|crowd|occasion)");
console.log("标签关联使用 RecipeTag 表");
console.log("");
console.log("请使用 admin API 或 check-tags-data.ts 查看标签状态");
process.exit(0);
