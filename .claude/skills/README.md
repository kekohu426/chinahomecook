# Claude Code Skills Framework

## Overview

这是一个可复用的 Claude Code Skills 框架，用于快速创建和管理自定义 skills。
专为独立开发者设计，覆盖从需求规划到上线运营的完整开发周期。

## 目录结构

```
.claude/skills/
├── README.md                # 本文件
├── _templates/              # Skill 模板文件
│   ├── SKILL.md.template
│   ├── reference.md.template
│   ├── examples.md.template
│   └── create-skill.sh      # 快速创建脚本
│
├── # 🎯 核心开发
├── superpowers/             # 核心命令集 (/brainstorm, /plan, /execute)
├── frontend-design/         # 前端设计指南 (避免AI味)
├── fullstack-engineer/      # 全栈开发模式
├── project-planner/         # 项目规划 (PRD, 技术设计)
│
├── # 🔧 工程实践
├── code-review/             # 代码审查
├── db-migrate/              # 数据库迁移
├── api-test/                # API 测试
├── git-flow/                # Git 工作流
├── playwright-testing/      # E2E 自动化测试
│
├── # 🚀 部署运维
├── devops-deploy/           # 部署运维 (Vercel/Docker/CI-CD)
├── seo-check/               # SEO 审计
│
├── # 📈 运营增长
├── ai-generate/             # AI 内容生成
├── content-seo/             # 内容创作与 SEO
└── analytics/               # 数据分析
```

## 已有 Skills (14个)

### 🎯 核心开发

| Skill | 触发词 | 功能 |
|-------|--------|------|
| `superpowers` | "/brainstorm", "/plan", "规划" | 项目规划执行工作流 |
| `frontend-design` | "UI", "设计", "样式" | 避免AI味的前端设计指南 |
| `fullstack-engineer` | "全栈", "开发功能" | Next.js 15 全栈开发模式 |
| `project-planner` | "PRD", "需求文档", "规划项目" | 从需求到技术方案 |

### 🔧 工程实践

| Skill | 触发词 | 功能 |
|-------|--------|------|
| `code-review` | "review", "审查代码" | 代码质量、安全、性能审查 |
| `db-migrate` | "migrate", "prisma" | Prisma 数据库迁移管理 |
| `api-test` | "test API", "测试接口" | API 端点测试 |
| `git-flow` | "commit", "PR", "提交" | Git 工作流操作 |
| `playwright-testing` | "E2E", "自动化测试" | Playwright 端到端测试 |

### 🚀 部署运维

| Skill | 触发词 | 功能 |
|-------|--------|------|
| `devops-deploy` | "部署", "上线", "CI/CD" | Vercel/Docker 部署指南 |
| `seo-check` | "SEO", "meta", "sitemap" | SEO 审计和优化 |

### 📈 运营增长

| Skill | 触发词 | 功能 |
|-------|--------|------|
| `ai-generate` | "生成食谱", "AI生成" | AI 内容生成 |
| `content-seo` | "内容创作", "关键词", "写文章" | 内容营销与 SEO |
| `analytics` | "数据分析", "埋点", "GA" | 数据追踪与分析 |

## 使用方法

### 触发 Skill

直接在对话中使用触发词：

```
"帮我 review 一下 RecipeCard 组件"
"执行数据库 migrate"
"测试一下 /api/recipes 接口"
```

### 查看可用 Skills

```
"What skills are available?"
```

## 创建新 Skill

### 1. 创建目录

```bash
mkdir -p .claude/skills/my-new-skill
```

### 2. 创建 SKILL.md

```yaml
---
name: my-new-skill
description: Clear description with trigger keywords
allowed-tools: Read, Grep, Glob
---

# My New Skill

## Instructions
...
```

### 3. 重启 Claude Code

新 skill 会自动加载。

## Skill 编写指南

### SKILL.md 必须包含

1. **YAML Front Matter**
   - `name`: 小写字母、数字、连字符（最多64字符）
   - `description`: 功能描述 + 触发词（最多1024字符）
   - `allowed-tools`: 可选，限制可用工具

2. **Instructions**: 清晰的步骤指南

3. **Examples**: 具体使用示例

### 最佳实践

- **description 要包含触发词**：用户会说的词语
- **保持在 500 行以内**：大文档拆分到 reference.md
- **脚本放在 scripts/ 目录**：执行脚本不消耗 tokens
- **使用具体示例**：不要抽象描述

### 工具权限

常用组合：
- **只读**: `Read, Grep, Glob`
- **编辑**: `Read, Write, Edit`
- **执行**: `Bash(npm:*), Bash(npx:*)`
- **完全访问**: 省略 `allowed-tools`

## 分享 Skill

### 项目内分享
将 `.claude/skills/` 提交到 Git

### 跨项目复用
复制 skill 目录到其他项目的 `.claude/skills/`

### 个人全局 Skill
放到 `~/.claude/skills/` 目录

## 故障排除

| 问题 | 解决方案 |
|------|----------|
| Skill 不触发 | 更新 description，添加更多触发词 |
| 文件未找到 | 检查 SKILL.md 大小写 |
| YAML 错误 | 确保用空格缩进，不要用 tab |
| 工具权限不足 | 检查 allowed-tools 配置 |

## 更新日志

- 2024-01-03: 初始化框架，创建 5 个基础 skills
