
# Recipe Zen 多语言后台开发需求文档 v2.0

## 📘 项目背景与核心设计理念

### 项目定位

Recipe Zen 是一个面向**国际用户**的中式美食食谱平台，但运营团队为中文母语者。系统需要解决的核心矛盾是：

- **运营侧**：需要用中文高效创建和管理内容

- **用户侧**：需要看到流畅的英文内容

- **技术要求**：两者必须实时同步，且对运营者透明

### 核心设计原则

1. **中文为源（Source of Truth）**：所有内容的原始版本均为中文，存储在主表中

2. **英文为衍生（Derived Content）**：英文内容由 AI 自动翻译生成，存储在翻译表中

3. **翻译自动化**：运营者无需手动翻译，系统自动完成

4. **翻译可干预**：AI 翻译结果可由运营者审核和修正

5. **架构前瞻性**：未来可轻松扩展到其他语言（日文、韩文等）

---

## 🏗️ 系统架构总览

### 多语言实现模式：主表 + 翻译表（Main Table + Translation Table）

```plaintext
┌─────────────────────────────────────────────────────────────┐
│                         数据流向图                           │
└─────────────────────────────────────────────────────────────┘

运营者（中文输入）
    ↓
┌──────────────────┐
│  后台管理界面     │ ← 纯中文UI
│  (Admin Panel)   │
└────────┬─────────┘
         │ 保存
         ↓
┌──────────────────┐
│   主表 (Main)    │ ← 存储中文内容
│  recipes         │
│  ingredients     │
│  tags            │
└────────┬─────────┘
         │ 触发翻译
         ↓
┌──────────────────┐
│  AI 翻译服务      │ ← OpenAI/Claude/DeepL
│  (Translation)   │
└────────┬─────────┘
         │ 返回译文
         ↓
┌──────────────────┐
│ 翻译表 (Trans)   │ ← 存储英文内容
│ recipe_trans     │    + 其他语言
│ ingredient_trans │
│ tag_trans        │
└────────┬─────────┘
         │ 用户请求
         ↓
┌──────────────────┐
│  前端展示层       │ ← 根据语言参数
│  (Frontend)      │    拉取对应语言
└──────────────────┘
         ↓
    国际用户（英文显示）
```

### 技术栈建议

**后端框架**

- **推荐**: Next.js 14+ (App Router) 或 NestJS

- **理由**:

  - 内置国际化支持（i18n）

  - 服务端渲染（SSR）有利于 SEO

  - API Routes 便于集成 AI 翻译服务

**数据库**

- **推荐**: PostgreSQL 15+

- **理由**:

  - 原生支持 JSON 字段（存储翻译状态、元数据）

  - 强大的全文搜索能力

  - 适合复杂查询（多表联查、翻译表关联）

**ORM**

- **推荐**: Prisma 或 TypeORM

- **理由**: 类型安全、迁移管理便捷

**AI 翻译服务**

- **主力**: OpenAI GPT-4 Turbo（支持 128k 上下文）

- **备用**: Anthropic Claude 3.5 Sonnet

- **降级方案**: DeepL API（通用文本）

**任务队列**

- **推荐**: BullMQ + Redis

- **理由**: 处理异步翻译任务，避免阻塞主流程

---

## 🗄️ 数据库设计（完整架构）

### 核心表结构

#### 1. 食谱主表（recipes）

```sql
CREATE TABLE recipes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  -- 核心内容（中文）
  name_zh VARCHAR(200) NOT NULL,              -- 菜名（中文）
  description_zh TEXT,                         -- 简介（中文）
  difficulty_zh VARCHAR(20),                   -- 难度：简单/中等/困难
  prep_time_minutes INT,                       -- 准备时间（分钟）
  cook_time_minutes INT,                       -- 烹饪时间（分钟）
  servings INT DEFAULT 2,                      -- 份量（人份）
  
  -- 多媒体
  cover_image_url TEXT,                        -- 封面图
  video_url TEXT,                              -- 视频链接（可选）
  
  -- 分类和标签
  category_id UUID REFERENCES categories(id),  -- 菜系（粤菜、川菜等）
  region_id UUID REFERENCES regions(id),       -- 地区（广东、四川等）
  
  -- 状态管理
  status VARCHAR(20) DEFAULT 'draft',          -- draft/published/archived
  is_featured BOOLEAN DEFAULT FALSE,           -- 是否精选
  is_seasonal BOOLEAN DEFAULT FALSE,           -- 是否时令
  season VARCHAR(20),                          -- spring/summer/autumn/winter
  
  -- 翻译状态
  translation_status JSONB DEFAULT '{}',       -- {"en": "completed", "ja": "pending"}
  last_translated_at TIMESTAMPTZ,              -- 最后翻译时间
  
  -- 审计字段
  created_by UUID REFERENCES admin_users(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  published_at TIMESTAMPTZ,
  
  -- 索引优化
  INDEX idx_status (status),
  INDEX idx_category (category_id),
  INDEX idx_featured (is_featured, published_at),
  INDEX idx_name_zh_gin (name_zh gin_trgm_ops)  -- 中文全文搜索
);
```

#### 2. 食谱翻译表（recipe_translations）

```sql
CREATE TABLE recipe_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_id UUID NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  
  -- 语言标识
  language_code VARCHAR(5) NOT NULL,           -- en, ja, ko, fr, etc.
  
  -- 翻译内容
  name TEXT NOT NULL,
  description TEXT,
  difficulty TEXT,
  
  -- 翻译元数据
  translation_method VARCHAR(20),              -- ai/manual/hybrid
  ai_model VARCHAR(50),                        -- gpt-4-turbo, claude-3.5-sonnet
  ai_prompt TEXT,                              -- 使用的提示词
  translation_quality_score DECIMAL(3,2),      -- 0.00-1.00
  
  -- 人工审核
  is_reviewed BOOLEAN DEFAULT FALSE,
  reviewed_by UUID REFERENCES admin_users(id),
  reviewed_at TIMESTAMPTZ,
  review_notes TEXT,
  
  -- 审计
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  -- 唯一约束：每个食谱的每种语言只能有一条翻译
  UNIQUE(recipe_id, language_code),
  INDEX idx_language (language_code, recipe_id)
);
```

#### 3. 食材主表（ingredients）

```sql
CREATE TABLE ingredients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  -- 核心信息（中文）
  name_zh VARCHAR(100) NOT NULL UNIQUE,        -- 食材名（如：五花肉）
  category VARCHAR(50),                        -- 分类：肉类/蔬菜/调料等
  unit_zh VARCHAR(20),                         -- 单位：克/个/勺
  
  -- 图标和图片
  icon_url TEXT,                               -- 小图标（用于食材列表）
  image_url TEXT,                              -- 大图片（用于详情页）
  
  -- 营养信息（可选）
  calories_per_100g DECIMAL(6,2),
  protein_per_100g DECIMAL(5,2),
  carbs_per_100g DECIMAL(5,2),
  
  -- 翻译状态
  translation_status JSONB DEFAULT '{}',
  
  -- 审计
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_category (category),
  INDEX idx_name_zh_gin (name_zh gin_trgm_ops)
);
```

#### 4. 食材翻译表（ingredient_translations）

```sql
CREATE TABLE ingredient_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ingredient_id UUID NOT NULL REFERENCES ingredients(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  name TEXT NOT NULL,                          -- 翻译后的食材名
  unit TEXT,                                   -- 翻译后的单位
  
  -- 翻译元数据
  translation_method VARCHAR(20),
  ai_model VARCHAR(50),
  is_reviewed BOOLEAN DEFAULT FALSE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(ingredient_id, language_code)
);
```

#### 5. 食谱食材关联表（recipe_ingredients）

```sql
CREATE TABLE recipe_ingredients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_id UUID NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  ingredient_id UUID NOT NULL REFERENCES ingredients(id),
  
  -- 用量（中文）
  quantity_zh VARCHAR(50),                     -- "200克" 或 "适量"
  notes_zh TEXT,                               -- 备注（如：切片、去皮）
  
  -- 分组（用于区分主料、辅料、调料）
  ingredient_group VARCHAR(50),                -- main/secondary/seasoning
  sort_order INT DEFAULT 0,                    -- 显示顺序
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_recipe (recipe_id, sort_order)
);
```

#### 6. 食谱食材用量翻译表（recipe_ingredient_translations）

```sql
CREATE TABLE recipe_ingredient_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_ingredient_id UUID NOT NULL REFERENCES recipe_ingredients(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  quantity TEXT,                               -- "200g" 或 "to taste"
  notes TEXT,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(recipe_ingredient_id, language_code)
);
```

#### 7. 烹饪步骤表（recipe_steps）

```sql
CREATE TABLE recipe_steps (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_id UUID NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  
  step_number INT NOT NULL,                    -- 步骤序号
  instruction_zh TEXT NOT NULL,                -- 步骤说明（中文）
  image_url TEXT,                              -- 步骤配图
  duration_minutes INT,                        -- 该步骤耗时
  
  -- 温度和火候（可选）
  temperature_zh VARCHAR(50),                  -- "中火" "180度"
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_recipe_step (recipe_id, step_number)
);
```

#### 8. 烹饪步骤翻译表（recipe_step_translations）

```sql
CREATE TABLE recipe_step_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_step_id UUID NOT NULL REFERENCES recipe_steps(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  instruction TEXT NOT NULL,
  temperature TEXT,
  
  translation_method VARCHAR(20),
  is_reviewed BOOLEAN DEFAULT FALSE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(recipe_step_id, language_code)
);
```

#### 9. 标签主表（tags）

```sql
CREATE TABLE tags (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  name_zh VARCHAR(50) NOT NULL UNIQUE,         -- 标签名（中文）如：快手菜、下饭
  type VARCHAR(30),                            -- feature/occasion/diet
  icon_name VARCHAR(50),                       -- 图标名（用于前端渲染）
  color VARCHAR(20),                           -- 标签颜色（hex）
  
  translation_status JSONB DEFAULT '{}',
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_type (type)
);
```

#### 10. 标签翻译表（tag_translations）

```sql
CREATE TABLE tag_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tag_id UUID NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  name TEXT NOT NULL,
  
  translation_method VARCHAR(20),
  is_reviewed BOOLEAN DEFAULT FALSE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(tag_id, language_code)
);
```

#### 11. 食谱标签关联表（recipe_tags）

```sql
CREATE TABLE recipe_tags (
  recipe_id UUID NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  tag_id UUID NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  PRIMARY KEY (recipe_id, tag_id)
);
```

#### 12. AI 翻译配置表（translation_configs）

```sql
CREATE TABLE translation_configs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  config_name VARCHAR(100) NOT NULL UNIQUE,    -- "recipe_name", "recipe_description"
  source_language VARCHAR(5) DEFAULT 'zh',
  target_languages TEXT[] DEFAULT ARRAY['en'], -- 目标语言数组
  
  -- AI 配置
  ai_provider VARCHAR(30) NOT NULL,            -- openai/anthropic/deepl
  ai_model VARCHAR(50) NOT NULL,               -- gpt-4-turbo
  api_key_encrypted TEXT NOT NULL,             -- 加密存储的 API Key
  
  -- 提示词模板
  system_prompt TEXT NOT NULL,
  user_prompt_template TEXT NOT NULL,          -- 含占位符：{content}
  
  -- 翻译参数
  temperature DECIMAL(3,2) DEFAULT 0.3,
  max_tokens INT DEFAULT 2000,
  
  -- 质量控制
  enable_quality_check BOOLEAN DEFAULT TRUE,
  min_quality_score DECIMAL(3,2) DEFAULT 0.70,
  
  -- 状态
  is_active BOOLEAN DEFAULT TRUE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### 13. 翻译任务队列表（translation_jobs）

```sql
CREATE TABLE translation_jobs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  -- 任务类型和目标
  entity_type VARCHAR(50) NOT NULL,            -- recipe/ingredient/tag/step
  entity_id UUID NOT NULL,                     -- 对应实体的 ID
  target_language VARCHAR(5) NOT NULL,
  
  -- 任务状态
  status VARCHAR(20) DEFAULT 'pending',        -- pending/processing/completed/failed
  priority INT DEFAULT 5,                      -- 1-10，数字越小优先级越高
  
  -- 执行信息
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  error_message TEXT,
  retry_count INT DEFAULT 0,
  max_retries INT DEFAULT 3,
  
  -- 结果
  translation_result JSONB,                    -- 存储翻译结果
  quality_score DECIMAL(3,2),
  
  -- 审计
  created_by UUID REFERENCES admin_users(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_status_priority (status, priority, created_at),
  INDEX idx_entity (entity_type, entity_id)
);
```

#### 14. 菜系分类表（categories）

```sql
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  name_zh VARCHAR(50) NOT NULL UNIQUE,         -- 粤菜、川菜、鲁菜等
  description_zh TEXT,
  icon_url TEXT,
  sort_order INT DEFAULT 0,
  
  translation_status JSONB DEFAULT '{}',
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_sort (sort_order)
);
```

#### 15. 菜系翻译表（category_translations）

```sql
CREATE TABLE category_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  category_id UUID NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  name TEXT NOT NULL,
  description TEXT,
  
  translation_method VARCHAR(20),
  is_reviewed BOOLEAN DEFAULT FALSE,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(category_id, language_code)
);
```

#### 16. 地区表（regions）

```sql
CREATE TABLE regions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  name_zh VARCHAR(50) NOT NULL UNIQUE,         -- 广东、四川、山东等
  translation_status JSONB DEFAULT '{}',
  
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE region_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  region_id UUID NOT NULL REFERENCES regions(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  name TEXT NOT NULL,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(region_id, language_code)
);
```

#### 17. 首页配置表（homepage_configs）

```sql
CREATE TABLE homepage_configs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  section_type VARCHAR(50) NOT NULL,           -- hero/featured/categories/seasonal
  title_zh VARCHAR(200),
  subtitle_zh TEXT,
  content_zh TEXT,                             -- JSON or Markdown
  
  -- 展示控制
  is_active BOOLEAN DEFAULT TRUE,
  sort_order INT DEFAULT 0,
  
  -- 关联内容
  featured_recipe_ids UUID[],                  -- 精选食谱 IDs
  
  translation_status JSONB DEFAULT '{}',
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  INDEX idx_section_order (section_type, sort_order)
);

CREATE TABLE homepage_config_translations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  config_id UUID NOT NULL REFERENCES homepage_configs(id) ON DELETE CASCADE,
  language_code VARCHAR(5) NOT NULL,
  
  title TEXT,
  subtitle TEXT,
  content TEXT,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(config_id, language_code)
);
```

#### 18. 管理员用户表（admin_users）

```sql
CREATE TABLE admin_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  email VARCHAR(255) NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  name VARCHAR(100) NOT NULL,
  role VARCHAR(30) DEFAULT 'editor',           -- admin/editor/translator
  
  -- 权限控制
  can_publish BOOLEAN DEFAULT FALSE,
  can_delete BOOLEAN DEFAULT FALSE,
  can_manage_translations BOOLEAN DEFAULT TRUE,
  
  -- 状态
  is_active BOOLEAN DEFAULT TRUE,
  last_login_at TIMESTAMPTZ,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🎛️ 后台功能需求详解

### 1. 食谱管理页面（Recipe Management）

#### 列表页面功能

**筛选和搜索**

- 按状态筛选：草稿/已发布/已归档

- 按菜系筛选：粤菜/川菜等

- 按地区筛选

- 按标签筛选（多选）

- 关键词搜索（菜名）

- 按翻译状态筛选：已翻译/未翻译/翻译中

**列表显示字段**

- 封面图（缩略图）

- 菜名（中文）+ 英文预览（hover 显示）

- 菜系 + 地区

- 难度 + 时间

- 状态标签

- 翻译状态图标（✅ 已完成 / ⏳ 进行中 / ❌ 未翻译）

- 操作按钮：编辑 / 预览 / 删除

**批量操作**

- 批量发布

- 批量设为精选

- 批量触发翻译

- 批量导出（CSV）

#### 编辑/新建页面布局

```plaintext
┌────────────────────────────────────────────────────────────┐
│  食谱编辑器                                   [保存草稿] [发布] │
├────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────────┐  │
│  │  中文内容（输入区）   │  │  英文预览（AI生成）       │  │
│  │                       │  │                           │  │
│  │  菜名：____________   │  │  Name: [AI generated]    │  │
│  │                       │  │                           │  │
│  │  简介：____________   │  │  Description:            │  │
│  │  ________________    │  │  [AI generated text...]  │  │
│  │                       │  │                           │  │
│  │  [上传封面图]         │  │  [Same image displayed]  │  │
│  │                       │  │                           │  │
│  │  菜系：[下拉选择]     │  │  Cuisine: [Translated]   │  │
│  │  地区：[下拉选择]     │  │  Region: [Translated]    │  │
│  │                       │  │                           │  │
│  │  难度：○简单 ○中等   │  │  Difficulty: Easy        │  │
│  │  准备时间：___分钟    │  │  Prep Time: ___ mins     │  │
│  │  烹饪时间：___分钟    │  │  Cook Time: ___ mins     │  │
│  │                       │  │                           │  │
│  └──────────────────────┘  └──────────────────────────┘  │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  食材清单（中文输入）          [+ 添加食材]         │   │
│  ├────────────────────────────────────────────────────┤   │
│  │  主料：                                             │   │
│  │  [食材搜索框▼] 五花肉  用量：200克  [删除]         │   │
│  │  [食材搜索框▼] 大葱    用量：1根    [删除]         │   │
│  │                                                      │   │
│  │  调料：                                             │   │
│  │  [食材搜索框▼] 生抽    用量：2勺    [删除]         │   │
│  │  ...                                                │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  烹饪步骤（中文输入）          [+ 添加步骤]         │   │
│  ├────────────────────────────────────────────────────┤   │
│  │  步骤 1: [文本框] 五花肉切块...  [上传图片] [删除] │   │
│  │  步骤 2: [文本框] 热锅下油...    [上传图片] [删除] │   │
│  │  ...                                                │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  标签选择（多选）                                   │   │
│  │  ☑ 快手菜  ☑ 下饭  ☐ 宴客  ☐ 时令                │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  翻译控制                                           │   │
│  │  目标语言：☑ 英文  ☐ 日文  ☐ 韩文                │   │
│  │  翻译模式：○ 自动翻译（保存时）                    │   │
│  │            ○ 手动触发                               │   │
│  │                                                      │   │
│  │  [🔄 立即翻译] [📝 查看翻译历史]                   │   │
│  └────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

#### 关键交互逻辑

**实时预览更新**

- 当运营者在左侧（中文区）输入内容时，右侧（英文预览区）显示"等待翻译..."

- 点击"立即翻译"按钮后：

  1. 发送 API 请求到翻译服务

  2. 显示加载动画（"AI 正在翻译中..."）

  3. 翻译完成后，右侧实时更新英文内容

  4. 如果翻译失败，显示错误提示并保留中文内容

**食材自动完成**

- 输入食材名时，从 `ingredients` 表查询匹配项

- 如果食材不存在，提示"该食材未录入，是否创建？"

- 创建新食材时，同步触发该食材的英文翻译

**保存行为**

```javascript
// 伪代码示例
async function saveRecipe(recipeData) {
  // 1. 保存中文内容到主表
  const recipe = await db.recipes.create({
    name_zh: recipeData.name_zh,
    description_zh: recipeData.description_zh,
    // ...其他字段
  });
  
  // 2. 保存食材关联
  for (const ingredient of recipeData.ingredients) {
    await db.recipe_ingredients.create({
      recipe_id: recipe.id,
      ingredient_id: ingredient.id,
      quantity_zh: ingredient.quantity_zh,
      notes_zh: ingredient.notes_zh,
    });
  }
  
  // 3. 保存烹饪步骤
  for (const step of recipeData.steps) {
    await db.recipe_steps.create({
      recipe_id: recipe.id,
      step_number: step.number,
      instruction_zh: step.instruction_zh,
    });
  }
  
  // 4. 触发翻译（异步）
  if (recipeData.auto_translate) {
    await enqueueTranslationJob({
      entity_type: 'recipe',
      entity_id: recipe.id,
      target_language: 'en',
      priority: 5,
    });
  }
  
  return recipe;
}
```

---

### 2. AI 配置管理页面（AI Translation Config）

#### 页面布局

```plaintext
┌────────────────────────────────────────────────────────────┐
│  AI 翻译配置                                    [+ 新建配置] │
├────────────────────────────────────────────────────────────┤
│                                                              │
│  配置列表：                                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 📄 recipe_name (食谱名称翻译)              [编辑]  │   │
│  │    模型: GPT-4 Turbo  |  状态: ✅ 活跃             │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ 📄 recipe_description (食谱简介翻译)        [编辑]  │   │
│  │    模型: Claude 3.5 Sonnet  |  状态: ✅ 活跃       │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ 📄 ingredient_name (食材名翻译)            [编辑]  │   │
│  │    模型: GPT-4 Turbo  |  状态: ✅ 活跃             │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  编辑配置: recipe_description                      │   │
│  ├────────────────────────────────────────────────────┤   │
│  │  配置名称：recipe_description                      │   │
│  │  源语言：  中文 (zh)                                │   │
│  │  目标语言：☑ 英文 (en)  ☐ 日文 (ja)  ☐ 韩文 (ko) │   │
│  │                                                      │   │
│  │  AI 服务商：                                        │   │
│  │  ○ OpenAI  ● Anthropic  ○ DeepL                   │   │
│  │                                                      │   │
│  │  模型选择：                                         │   │
│  │  ▼ Claude 3.5 Sonnet                               │   │
│  │                                                      │   │
│  │  API Key：                                          │   │
│  │  [sk-ant-************************] [测试连接]       │   │
│  │                                                      │   │
│  │  系统提示词（System Prompt）：                      │   │
│  │  ┌──────────────────────────────────────────┐     │   │
│  │  │ You are a professional translator         │     │   │
│  │  │ specializing in Chinese cuisine. Translate │     │   │
│  │  │ the following recipe description from     │     │   │
│  │  │ Chinese to English...                      │     │   │
│  │  └──────────────────────────────────────────┘     │   │
│  │                                                      │   │
│  │  用户提示词模板（User Prompt Template）：          │   │
│  │  ┌──────────────────────────────────────────┐     │   │
│  │  │ Translate: {content}                       │     │   │
│  │  │                                             │     │   │
│  │  │ Requirements:                               │     │   │
│  │  │ - Keep the tone warm and inviting          │     │   │
│  │  │ - Use natural, fluent English              │     │   │
│  │  │ - Preserve cultural context                │     │   │
│  │  └──────────────────────────────────────────┘     │   │
│  │                                                      │   │
│  │  高级参数：                                         │   │
│  │  Temperature: [━━━●━━━━━━] 0.3                    │   │
│  │  Max Tokens: [2000]                                │   │
│  │                                                      │   │
│  │  质量控制：                                         │   │
│  │  ☑ 启用质量检查                                    │   │
│  │  最低质量分数：[━━━━━●━━━━] 0.70                  │   │
│  │                                                      │   │
│  │  [测试翻译]  [保存配置]  [取消]                    │   │
│  └────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

#### 测试翻译功能

```plaintext
┌────────────────────────────────────────────────────────────┐
│  测试翻译                                             [关闭] │
├────────────────────────────────────────────────────────────┤
│  输入测试文本（中文）：                                      │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 这道红烧肉色泽红亮，肥而不腻，入口即化，是一道       │   │
│  │ 经典的家常菜，特别适合配米饭食用。                   │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  [🚀 开始翻译]                                              │
│                                                              │
│  翻译结果（英文）：                                          │
│  ┌────────────────────────────────────────────────────┐   │
│  │ This braised pork dish features a rich red glaze,   │   │
│  │ offering tender, melt-in-your-mouth meat that's    │   │
│  │ flavorful without being greasy. A classic home-    │   │
│  │ style recipe, it pairs perfectly with steamed rice.│   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  翻译质量评分：⭐⭐⭐⭐⭐ (0.92)                            │
│  使用模型：Claude 3.5 Sonnet                                │
│  耗时：1.2秒  |  Tokens: 85                                 │
└────────────────────────────────────────────────────────────┘
```

---

### 3. 食材与图标管理页面（Ingredients & Icons）

#### 食材列表页面

```plaintext
┌────────────────────────────────────────────────────────────┐
│  食材管理                              [+ 新建食材] [导入CSV] │
├────────────────────────────────────────────────────────────┤
│  搜索：[____________________] [搜索]                        │
│  分类：[全部▼] [肉类] [蔬菜] [调料] [其他]                  │
│  翻译状态：[全部▼] [已翻译] [未翻译] [翻译中]               │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 图标 │ 中文名 │ 英文名       │ 分类 │ 翻译 │ 操作   │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ 🥩  │ 五花肉 │ Pork Belly   │ 肉类 │ ✅   │ [编辑] │   │
│  │ 🧅  │ 大葱   │ Scallion     │ 蔬菜 │ ✅   │ [编辑] │   │
│  │ 🍶  │ 生抽   │ Light Soy... │ 调料 │ ✅   │ [编辑] │   │
│  │ 🌶️  │ 辣椒   │ Chili Pepper │ 蔬菜 │ ⏳   │ [编辑] │   │
│  │ ...                                                  │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  共 328 个食材  |  已翻译: 305  |  未翻译: 23                 │
└────────────────────────────────────────────────────────────┘
```

#### 食材编辑弹窗

```plaintext
┌────────────────────────────────────────────────────────────┐
│  编辑食材: 五花肉                                     [关闭] │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────────┐  │
│  │  中文信息              │  │  英文预览                 │  │
│  │                       │  │                           │  │
│  │  名称：五花肉         │  │  Name: Pork Belly        │  │
│  │  分类：[肉类▼]        │  │  Category: Meat          │  │
│  │  单位：克             │  │  Unit: gram              │  │
│  │                       │  │                           │  │
│  │  图标上传：           │  │  Icon Preview:           │  │
│  │  ┌────────┐          │  │  ┌────────┐             │  │
│  │  │  🥩   │ [更换]   │  │  │  🥩   │             │  │
│  │  └────────┘          │  │  └────────┘             │  │
│  │                       │  │                           │  │
│  │  大图上传：           │  │  Image Preview:          │  │
│  │  [拖拽或点击上传]     │  │  [Same image displayed]  │  │
│  │                       │  │                           │  │
│  └──────────────────────┘  └──────────────────────────┘  │
│                                                              │
│  营养信息（可选）：                                          │
│  每 100g: 热量 [_____] 卡路里  |  蛋白质 [_____] g           │
│                                                              │
│  翻译状态：✅ 已翻译（2024-01-05 10:30）                    │
│  [🔄 重新翻译] [📝 手动编辑英文]                            │
│                                                              │
│  [保存] [删除] [取消]                                        │
└────────────────────────────────────────────────────────────┘
```

#### 图标库管理（单独页面）

```plaintext
┌────────────────────────────────────────────────────────────┐
│  食材图标库                          [+ 上传图标] [批量上传] │
├────────────────────────────────────────────────────────────┤
│  筛选：[未使用] [已使用] [全部]                              │
│  搜索：[图标名称或关键词________________] [搜索]            │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  🥩  pork.svg         已用于: 五花肉, 猪肉         │   │
│  │  🧅  scallion.svg     已用于: 大葱                 │   │
│  │  🍶  soy-sauce.svg    已用于: 生抽, 老抽           │   │
│  │  🌶️  chili.svg        已用于: 辣椒                 │   │
│  │  🧄  garlic.svg       未使用                       │   │
│  │  🥕  carrot.svg       未使用                       │   │
│  │  ...                                                │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  提示：支持 SVG、PNG 格式，建议尺寸 64x64px                  │
└────────────────────────────────────────────────────────────┘
```

---

### 4. 标签管理页面（Tags Management）

```plaintext
┌────────────────────────────────────────────────────────────┐
│  标签管理                                       [+ 新建标签] │
├────────────────────────────────────────────────────────────┤
│  类型筛选：[全部] [特色标签] [场合标签] [饮食类型]          │
│                                                              │
│  特色标签：                                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │ [快手菜] Quick Meal     ✅ 已翻译  [编辑] [删除]   │   │
│  │ [下饭]   Goes Well...   ✅ 已翻译  [编辑] [删除]   │   │
│  │ [养生]   Nourishing     ⏳ 翻译中  [编辑] [删除]   │   │
│  │ [时令]   Seasonal       ✅ 已翻译  [编辑] [删除]   │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  场合标签：                                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │ [宴客]   For Guests     ✅ 已翻译  [编辑] [删除]   │   │
│  │ [早餐]   Breakfast      ✅ 已翻译  [编辑] [删除]   │   │
│  │ [夜宵]   Late-Night...  ✅ 已翻译  [编辑] [删除]   │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  饮食类型：                                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │ [素食]   Vegetarian     ✅ 已翻译  [编辑] [删除]   │   │
│  │ [低卡]   Low Calorie    ✅ 已翻译  [编辑] [删除]   │   │
│  └────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

#### 标签编辑弹窗

```plaintext
┌────────────────────────────────────────────────────────────┐
│  编辑标签: 快手菜                                     [关闭] │
├────────────────────────────────────────────────────────────┤
│  中文名称：快手菜                                            │
│  英文名称：Quick Meal  [🔄 重新翻译]                        │
│                                                              │
│  标签类型：● 特色标签  ○ 场合标签  ○ 饮食类型              │
│                                                              │
│  图标名称：[clock-fast▼]  预览: ⏱️                         │
│  标签颜色：[#FF6B6B] ■ [选择颜色]                           │
│                                                              │
│  使用该标签的食谱数：127                                     │
│                                                              │
│  [保存] [删除] [取消]                                        │
└────────────────────────────────────────────────────────────┘
```

---

### 5. 首页配置页面（Homepage Configuration）

```plaintext
┌────────────────────────────────────────────────────────────┐
│  首页配置                                  [+ 添加区块] [预览] │
├────────────────────────────────────────────────────────────┤
│  语言视图：● 中文编辑  ○ 英文预览                           │
│                                                              │
│  区块列表（拖拽排序）：                                      │
│  ┌────────────────────────────────────────────────────┐   │
│  │ ≡ 顶部横幅（Hero Banner）              ✅ [编辑]    │   │
│  │   标题: "发现中国美食的治愈力量"                    │   │
│  │   副标题: "让每一餐都充满温暖与..."                 │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ ≡ 精选食谱（Featured Recipes）         ✅ [编辑]    │   │
│  │   已选择 6 个食谱                                   │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ ≡ 菜系分类（Cuisine Categories）       ✅ [编辑]    │   │
│  │   显示: 粤菜、川菜、鲁菜、湘菜                      │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ ≡ 时令推荐（Seasonal Picks）           ✅ [编辑]    │   │
│  │   当前季节: 冬季  |  已选 4 个食谱                  │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ ≡ 底部介绍（About Section）            ⏳ [编辑]    │   │
│  │   标题: "我们的理念"  |  待翻译                     │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  [保存全部更改] [恢复默认配置]                               │
└────────────────────────────────────────────────────────────┘
```

#### 编辑横幅区块

```plaintext
┌────────────────────────────────────────────────────────────┐
│  编辑横幅区块                                         [关闭] │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────────┐  │
│  │  中文内容              │  │  英文预览                 │  │
│  │                       │  │                           │  │
│  │  标题：               │  │  Title:                   │  │
│  │  发现中国美食的治愈   │  │  Discover the Healing... │  │
│  │  力量                 │  │                           │  │
│  │                       │  │                           │  │
│  │  副标题：             │  │  Subtitle:                │  │
│  │  让每一餐都充满温暖   │  │  Bring warmth and...     │  │
│  │  与心意               │  │                           │  │
│  │                       │  │                           │  │
│  │  按钮文字：           │  │  Button Text:             │  │
│  │  开始探索             │  │  Start Exploring         │  │
│  │                       │  │                           │  │
│  │  背景图：             │  │  Background:              │  │
│  │  [上传或选择]         │  │  [Same image]            │  │
│  │                       │  │                           │  │
│  └──────────────────────┘  └──────────────────────────┘  │
│                                                              │
│  显示设置：                                                  │
│  ☑ 在首页显示  |  高度: [大▼]                               │
│                                                              │
│  [🔄 翻译预览] [保存] [取消]                                │
└────────────────────────────────────────────────────────────┘
```

---

### 6. 图片库管理页面（Image Library）

```plaintext
┌────────────────────────────────────────────────────────────┐
│  图片库                          [上传图片] [批量上传] [导入] │
├────────────────────────────────────────────────────────────┤
│  筛选：[全部] [食谱图片] [食材图片] [横幅图片] [未分类]      │
│  排序：[上传时间▼]  视图：[网格■] [列表☰]                   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐        │   │
│  │  │ 🖼️    │ │ 🖼️    │ │ 🖼️    │ │ 🖼️    │        │   │
│  │  │ 红烧肉 │ │ 宫保... │ │ 麻婆... │ │ 蒜蓉... │        │   │
│  │  │ 1.2MB │ │ 980KB │ │ 1.5MB │ │ 800KB │        │   │
│  │  │ [选择] │ │ [选择] │ │ [选择] │ │ [选择] │        │   │
│  │  └───────┘ └───────┘ └───────┘ └───────┘        │   │
│  │                                                      │   │
│  │  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐        │   │
│  │  │ 🖼️    │ │ 🖼️    │ │ 🖼️    │ │ 🖼️    │        │   │
│  │  │ 五花肉 │ │ 大葱   │ │ 生姜   │ │ 大蒜   │        │   │
│  │  │ 500KB │ │ 450KB │ │ 520KB │ │ 480KB │        │   │
│  │  │ [选择] │ │ [选择] │ │ [选择] │ │ [选择] │        │   │
│  │  └───────┘ └───────┘ └───────┘ └───────┘        │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  已选择 0 项  |  [批量删除] [批量移动到...]                   │
└────────────────────────────────────────────────────────────┘
```

#### 图片详情弹窗

```plaintext
┌────────────────────────────────────────────────────────────┐
│  图片详情                                             [关闭] │
├────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────┐           │
│  │                                             │           │
│  │          [图片预览 - 红烧肉.jpg]            │           │
│  │                                             │           │
│  │               1920 x 1080                   │           │
│  │                                             │           │
│  └────────────────────────────────────────────┘           │
│                                                              │
│  文件名：hongshaorou.jpg                                     │
│  大小：1.2 MB                                                │
│  上传时间：2024-01-05 10:30:45                               │
│  上传者：keko hu                                             │
│                                                              │
│  使用情况：                                                  │
│  • 食谱《红烧肉》- 封面图                                    │
│  • 食谱《红烧肉》- 步骤 3 配图                               │
│                                                              │
│  图片 URL（可复制）：                                        │
│  [https://cdn.recipe-zen.com/images/...] [复制]             │
│                                                              │
│  [替换图片] [删除] [关闭]                                    │
└────────────────────────────────────────────────────────────┘
```

---

### 7. 翻译任务管理页面（Translation Jobs）

```plaintext
┌────────────────────────────────────────────────────────────┐
│  翻译任务队列                                   [刷新] [清空] │
├────────────────────────────────────────────────────────────┤
│  状态：[全部] [进行中] [已完成] [失败] [待处理]              │
│  类型：[全部] [食谱] [食材] [标签] [步骤]                    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 任务 │ 类型 │ 内容           │ 语言 │ 状态 │ 操作   │   │
│  ├────────────────────────────────────────────────────┤   │
│  │ #001 │ 食谱 │ 红烧肉         │ en   │ ⏳进行│ [查看] │   │
│  │ #002 │ 步骤 │ 红烧肉-步骤1   │ en   │ ✅完成│ [查看] │   │
│  │ #003 │ 食材 │ 五花肉         │ en   │ ✅完成│ [查看] │   │
│  │ #004 │ 食谱 │ 宫保鸡丁       │ en   │ ❌失败│ [重试] │   │
│  │ #005 │ 标签 │ 快手菜         │ en   │ ⏸️待处│ [执行] │   │
│  │ ...                                                  │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  统计：进行中 5  |  已完成 128  |  失败 3  |  待处理 12        │
└────────────────────────────────────────────────────────────┘
```

#### 任务详情弹窗

```plaintext
┌────────────────────────────────────────────────────────────┐
│  翻译任务详情 #004                                    [关闭] │
├────────────────────────────────────────────────────────────┤
│  任务 ID: #004                                               │
│  类型: 食谱                                                  │
│  实体: 宫保鸡丁 (recipe_id: abc123...)                       │
│  目标语言: 英文 (en)                                         │
│  优先级: 5                                                   │
│                                                              │
│  状态: ❌ 失败                                               │
│  错误信息:                                                   │
│  ┌────────────────────────────────────────────────────┐   │
│  │ API Rate Limit Exceeded (429)                       │   │
│  │ OpenAI API 请求次数超限，请稍后重试                 │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  重试次数: 2 / 3                                             │
│  创建时间: 2024-01-05 10:30:00                               │
│  开始时间: 2024-01-05 10:30:05                               │
│  失败时间: 2024-01-05 10:30:15                               │
│                                                              │
│  [🔄 立即重试] [📝 查看日志] [🗑️ 删除任务]                  │
└────────────────────────────────────────────────────────────┘
```

---

### 8. 系统设置页面（System Settings）

```plaintext
┌────────────────────────────────────────────────────────────┐
│  系统设置                                                    │
├────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐   │
│  │ 👤 账号设置                                         │   │
│  │                                                      │   │
│  │ 管理员信息：                                        │   │
│  │ 姓名: keko hu                                        │   │
│  │ 邮箱: keko@recipezen.com                            │   │
│  │ 角色: 超级管理员                                    │   │
│  │                                                      │   │
│  │ [修改密码] [编辑资料]                               │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 🌐 多语言设置                                       │   │
│  │                                                      │   │
│  │ 默认源语言: 中文 (zh)                               │   │
│  │ 启用的目标语言:                                     │   │
│  │ ☑ 英文 (en) - 主要语言                             │   │
│  │ ☐ 日文 (ja)                                         │   │
│  │ ☐ 韩文 (ko)                                         │   │
│  │ ☐ 法文 (fr)                                         │   │
│  │                                                      │   │
│  │ 自动翻译: ☑ 启用                                    │   │
│  │ 翻译触发时机: ○ 保存时  ● 发布时  ○ 手动           │   │
│  │                                                      │   │
│  │ [保存设置]                                          │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 🔒 安全设置                                         │   │
│  │                                                      │   │
│  │ API 密钥管理：                                      │   │
│  │ OpenAI: [sk-********************] [更新] [测试]     │   │
│  │ Anthropic: [sk-ant-*************] [更新] [测试]     │   │
│  │ DeepL: [未配置] [添加]                              │   │
│  │                                                      │   │
│  │ 会话超时: [30▼] 分钟                                │   │
│  │ 双因素认证: ☑ 启用                                  │   │
│  │                                                      │   │
│  │ [保存设置]                                          │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │ 📊 数据管理                                         │   │
│  │                                                      │   │
│  │ 数据导出:                                           │   │
│  │ [导出全部食谱] [导出食材库] [导出翻译记录]         │   │
│  │                                                      │   │
│  │ 数据导入:                                           │   │
│  │ [导入食谱] [导入食材] [导入标签]                    │   │
│  │                                                      │   │
│  │ 数据库备份:                                         │   │
│  │ 最后备份: 2024-01-05 02:00:00                       │   │
│  │ [立即备份] [查看备份历史]                           │   │
│  └────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

---

## 🌍 前端路由与语言切换机制

### 路由设计方案

#### 方案 A：基于子路径（推荐）

```plaintext
网站结构：
https://recipezen.com/
  ├── /en/                      # 英文版（默认）
  │   ├── /en/recipes           # 食谱列表
  │   ├── /en/recipes/[slug]    # 食谱详情（如 /en/recipes/braised-pork）
  │   ├── /en/cuisines          # 菜系列表
  │   ├── /en/cuisines/[slug]   # 菜系详情（如 /en/cuisines/cantonese）
  │   ├── /en/about             # 关于我们
  │   └── /en/blog              # 博客
  │
  └── /zh/                      # 中文版（可选，为SEO优化）
      ├── /zh/recipes
      ├── /zh/recipes/[slug]    # 如 /zh/recipes/hong-shao-rou
      └── ...
```

**实现逻辑（Next.js 示例）**

```typescript
// middleware.ts
import { NextRequest, NextResponse } from 'next/server';

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;
  
  // 如果已经包含语言前缀，直接放行
  if (pathname.startsWith('/en/') || pathname.startsWith('/zh/')) {
    return NextResponse.next();
  }
  
  // 检测用户浏览器语言
  const acceptLanguage = request.headers.get('accept-language') || '';
  const preferredLang = acceptLanguage.startsWith('zh') ? 'zh' : 'en';
  
  // 检查 Cookie 中的语言偏好
  const cookieLang = request.cookies.get('preferred_language')?.value;
  const targetLang = cookieLang || preferredLang;
  
  // 重定向到对应语言版本
  const url = request.nextUrl.clone();
  url.pathname = `/${targetLang}${pathname}`;
  return NextResponse.redirect(url);
}

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

#### 方案 B：基于子域名（备选）

```plaintext
英文版: https://recipezen.com（或 en.recipezen.com）
中文版: https://zh.recipezen.com
日文版: https://ja.recipezen.com
```

**优点**: 完全独立，利于 SEO\
**缺点**: 需要额外的域名配置和 SSL 证书

---

### 语言检测优先级

```plaintext
┌─────────────────────────────────────────┐
│  用户访问 https://recipezen.com/       │
└────────────┬────────────────────────────┘
             │
             ↓
    1. 检查 URL 参数: ?lang=zh
             │
             ↓ 无
    2. 检查 Cookie: preferred_language
             │
             ↓ 无
    3. 检查浏览器 Accept-Language Header
             │
             ↓
    4. 默认使用 English (en)
             │
             ↓
    重定向到 /en/ 或 /zh/
```

---

### SEO 优化配置

#### 1. Hreflang 标签（关键！）

在每个页面的 `<head>` 中添加：

```html
<!-- 食谱详情页: 红烧肉 --><link rel="alternate" hreflang="en" href="https://recipezen.com/en/recipes/braised-pork"><link rel="alternate" hreflang="zh" href="https://recipezen.com/zh/recipes/hong-shao-rou"><link rel="alternate" hreflang="x-default" href="https://recipezen.com/en/recipes/braised-pork">
```

**作用**: 告诉搜索引擎同一内容的不同语言版本

#### 2. 语言切换组件

```typescript
// LanguageSwitcher.tsx
export function LanguageSwitcher({ currentPath }: { currentPath: string }) {
  const languages = [
    { code: 'en', name: 'English', flag: '🇺🇸' },
    { code: 'zh', name: '中文', flag: '🇨🇳' },
  ];
  
  const switchLanguage = (targetLang: string) => {
    // 替换 URL 中的语言代码
    const newPath = currentPath.replace(/^\/(en|zh)\//, `/${targetLang}/`);
    
    // 设置 Cookie
    document.cookie = `preferred_language=${targetLang}; path=/; max-age=31536000`;
    
    // 跳转
    window.location.href = newPath;
  };
  
  return (
    <div classname="language-switcher">
      {languages.map(lang => (
        <button key="{lang.code}" onclick="{()" ==""> switchLanguage(lang.code)}>
          {lang.flag} {lang.name}
        </button>
      ))}
    </div>
  );
}
```

#### 3. 动态 Sitemap

生成两个 sitemap（或合并为一个）：

```html
<!-- sitemap-en.xml --><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml"><url>
    <loc>https://recipezen.com/en/recipes/braised-pork</loc>
    <xhtml:link rel="alternate" hreflang="zh" href="https://recipezen.com/zh/recipes/hong-shao-rou">
    <xhtml:link rel="alternate" hreflang="en" href="https://recipezen.com/en/recipes/braised-pork"></xhtml:link></xhtml:link></url><!-- 其他页面 --></urlset>
```

---

### 前端数据获取逻辑

#### 示例：食谱详情页

```typescript
// app/[lang]/recipes/[slug]/page.tsx
interface RecipePageProps {
  params: {
    lang: string;
    slug: string;
  };
}

export default async function RecipePage({ params }: RecipePageProps) {
  const { lang, slug } = params;
  
  // 1. 根据 slug 查询食谱主记录
  const recipe = await db.query.recipes.findFirst({
    where: eq(recipes.slug, slug),
    with: {
      category: true,
      region: true,
      ingredients: true,
      steps: true,
      tags: true,
    },
  });
  
  if (!recipe) {
    notFound();
  }
  
  // 2. 获取对应语言的翻译内容
  const translation = await db.query.recipe_translations.findFirst({
    where: and(
      eq(recipe_translations.recipe_id, recipe.id),
      eq(recipe_translations.language_code, lang)
    ),
  });
  
  // 3. 合并数据（如果翻译存在，使用翻译；否则使用中文原文）
  const displayData = {
    name: translation?.name || recipe.name_zh,
    description: translation?.description || recipe.description_zh,
    difficulty: translation?.difficulty || recipe.difficulty_zh,
    // ...其他字段
  };
  
  // 4. 渲染页面
  return (
    <div>
      <h1>{displayData.name}</h1>
      <p>{displayData.description}</p>
      {/* ...其他内容 */}
    </div>
  );
}

// 5. 生成所有可能的静态路径（用于 SSG）
export async function generateStaticParams() {
  const recipes = await db.query.recipes.findMany();
  const langs = ['en', 'zh'];
  
  const paths = [];
  for (const recipe of recipes) {
    for (const lang of langs) {
      paths.push({
        lang,
        slug: recipe.slug,
      });
    }
  }
  
  return paths;
}
```

---

## 🤖 AI 翻译集成详解

### 翻译触发时机

#### 1. 自动触发（推荐）

**场景**: 运营者点击"发布"按钮时

```typescript
async function publishRecipe(recipeId: string) {
  // 1. 更新食谱状态为已发布
  await db.update(recipes)
    .set({ status: 'published', published_at: new Date() })
    .where(eq(recipes.id, recipeId));
  
  // 2. 创建翻译任务（异步）
  const targetLanguages = ['en']; // 可从系统配置读取
  
  for (const lang of targetLanguages) {
    await enqueueTranslationJob({
      entity_type: 'recipe',
      entity_id: recipeId,
      target_language: lang,
      priority: 1, // 发布时优先级最高
    });
    
    // 同时为食材、步骤创建翻译任务
    await enqueueRecipeComponentsTranslation(recipeId, lang);
  }
  
  return { success: true };
}
```

#### 2. 手动触发

**场景**: 运营者在编辑页面点击"立即翻译"按钮

```typescript
async function manualTranslate(recipeId: string, targetLang: string) {
  // 创建高优先级任务
  const job = await enqueueTranslationJob({
    entity_type: 'recipe',
    entity_id: recipeId,
    target_language: targetLang,
    priority: 1,
  });
  
  // 立即执行（不等待队列）
  await executeTranslationJob(job.id);
  
  return { success: true, jobId: job.id };
}
```

#### 3. 批量翻译

**场景**: 运营者在列表页面勾选多个食谱，点击"批量翻译"

```typescript
async function batchTranslate(recipeIds: string[], targetLang: string) {
  for (const recipeId of recipeIds) {
    await enqueueTranslationJob({
      entity_type: 'recipe',
      entity_id: recipeId,
      target_language: targetLang,
      priority: 5, // 批量任务优先级较低
    });
  }
  
  return { success: true, totalJobs: recipeIds.length };
}
```

---

### 翻译任务队列实现（BullMQ）

#### 1. 任务入队

```typescript
// services/translation-queue.ts
import { Queue } from 'bullmq';
import { Redis } from 'ioredis';

const redis = new Redis({
  host: process.env.REDIS_HOST,
  port: parseInt(process.env.REDIS_PORT || '6379'),
});

export const translationQueue = new Queue('translation-jobs', {
  connection: redis,
});

export async function enqueueTranslationJob(data: {
  entity_type: string;
  entity_id: string;
  target_language: string;
  priority: number;
}) {
  // 1. 检查是否已存在相同任务
  const existingJob = await db.query.translation_jobs.findFirst({
    where: and(
      eq(translation_jobs.entity_type, data.entity_type),
      eq(translation_jobs.entity_id, data.entity_id),
      eq(translation_jobs.target_language, data.target_language),
      eq(translation_jobs.status, 'pending')
    ),
  });
  
  if (existingJob) {
    return existingJob; // 避免重复创建
  }
  
  // 2. 创建数据库记录
  const [job] = await db.insert(translation_jobs).values({
    entity_type: data.entity_type,
    entity_id: data.entity_id,
    target_language: data.target_language,
    priority: data.priority,
    status: 'pending',
  }).returning();
  
  // 3. 加入队列
  await translationQueue.add('translate', {
    jobId: job.id,
  }, {
    priority: 11 - data.priority, // BullMQ 优先级反向（数字越小越优先）
    attempts: 3,
    backoff: {
      type: 'exponential',
      delay: 5000, // 失败后 5 秒重试
    },
  });
  
  return job;
}
```

#### 2. 任务处理 Worker

```typescript
// workers/translation-worker.ts
import { Worker } from 'bullmq';
import { translateRecipe, translateIngredient, translateStep } from './translation-service';

export const translationWorker = new Worker('translation-jobs', async (job) => {
  const { jobId } = job.data;
  
  // 1. 获取任务详情
  const translationJob = await db.query.translation_jobs.findFirst({
    where: eq(translation_jobs.id, jobId),
  });
  
  if (!translationJob) {
    throw new Error(`Translation job ${jobId} not found`);
  }
  
  try {
    // 2. 更新状态为"进行中"
    await db.update(translation_jobs)
      .set({ status: 'processing', started_at: new Date() })
      .where(eq(translation_jobs.id, jobId));
    
    // 3. 根据实体类型调用对应的翻译函数
    let result;
    switch (translationJob.entity_type) {
      case 'recipe':
        result = await translateRecipe(translationJob.entity_id, translationJob.target_language);
        break;
      case 'ingredient':
        result = await translateIngredient(translationJob.entity_id, translationJob.target_language);
        break;
      case 'step':
        result = await translateStep(translationJob.entity_id, translationJob.target_language);
        break;
      default:
        throw new Error(`Unknown entity type: ${translationJob.entity_type}`);
    }
    
    // 4. 更新任务状态为"完成"
    await db.update(translation_jobs)
      .set({
        status: 'completed',
        completed_at: new Date(),
        translation_result: result,
        quality_score: result.quality_score,
      })
      .where(eq(translation_jobs.id, jobId));
    
    return result;
    
  } catch (error) {
    // 5. 处理错误
    await db.update(translation_jobs)
      .set({
        status: 'failed',
        error_message: error.message,
        retry_count: translationJob.retry_count + 1,
      })
      .where(eq(translation_jobs.id, jobId));
    
    throw error; // 重新抛出，让 BullMQ 处理重试
  }
}, {
  connection: redis,
  concurrency: 5, // 同时处理 5 个任务
});
```

---

### 翻译服务实现

#### 核心翻译函数

```typescript
// services/translation-service.ts
import OpenAI from 'openai';
import Anthropic from '@anthropic-ai/sdk';

export async function translateRecipe(recipeId: string, targetLang: string) {
  // 1. 获取食谱原始数据
  const recipe = await db.query.recipes.findFirst({
    where: eq(recipes.id, recipeId),
  });
  
  if (!recipe) {
    throw new Error(`Recipe ${recipeId} not found`);
  }
  
  // 2. 获取翻译配置
  const config = await db.query.translation_configs.findFirst({
    where: and(
      eq(translation_configs.config_name, 'recipe_description'),
      eq(translation_configs.is_active, true)
    ),
  });
  
  if (!config) {
    throw new Error('Translation config not found');
  }
  
  // 3. 构建提示词
  const systemPrompt = config.system_prompt;
  const userPrompt = config.user_prompt_template
    .replace('{name}', recipe.name_zh)
    .replace('{description}', recipe.description_zh);
  
  // 4. 调用 AI API
  let translatedName, translatedDescription;
  
  if (config.ai_provider === 'openai') {
    const openai = new OpenAI({ apiKey: decryptApiKey(config.api_key_encrypted) });
    
    const completion = await openai.chat.completions.create({
      model: config.ai_model,
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: userPrompt },
      ],
      temperature: config.temperature,
      max_tokens: config.max_tokens,
    });
    
    const result = completion.choices[0].message.content;
    const parsed = JSON.parse(result); // 假设 AI 返回 JSON 格式
    translatedName = parsed.name;
    translatedDescription = parsed.description;
  } else if (config.ai_provider === 'anthropic') {
    const anthropic = new Anthropic({ apiKey: decryptApiKey(config.api_key_encrypted) });
    
    const message = await anthropic.messages.create({
      model: config.ai_model,
      max_tokens: config.max_tokens,
      system: systemPrompt,
      messages: [{ role: 'user', content: userPrompt }],
    });
    
    const result = message.content[0].text;
    const parsed = JSON.parse(result);
    translatedName = parsed.name;
    translatedDescription = parsed.description;
  }
  
  // 5. 质量评分（简化版：基于长度比例）
  const qualityScore = Math.min(
    translatedDescription.length / recipe.description_zh.length,
    1.0
  );
  
  // 6. 保存翻译结果
  await db.insert(recipe_translations).values({
    recipe_id: recipeId,
    language_code: targetLang,
    name: translatedName,
    description: translatedDescription,
    translation_method: 'ai',
    ai_model: config.ai_model,
    ai_prompt: systemPrompt + '

' + userPrompt,
    translation_quality_score: qualityScore,
  }).onConflictDoUpdate({
    target: [recipe_translations.recipe_id, recipe_translations.language_code],
    set: {
      name: translatedName,
      description: translatedDescription,
      translation_quality_score: qualityScore,
      updated_at: new Date(),
    },
  });
  
  // 7. 更新主表翻译状态
  const currentStatus = recipe.translation_status || {};
  currentStatus[targetLang] = 'completed';
  
  await db.update(recipes)
    .set({
      translation_status: currentStatus,
      last_translated_at: new Date(),
    })
    .where(eq(recipes.id, recipeId));
  
  return {
    name: translatedName,
    description: translatedDescription,
    quality_score: qualityScore,
  };
}
```

#### 提示词模板示例

**食谱名称翻译**

```json
{
  "system_prompt": "You are a professional translator specializing in Chinese cuisine. Your task is to translate Chinese dish names into English. The translation should be:
- Accurate and culturally appropriate
- Easy for English speakers to understand
- Consistent with common culinary terminology

Return ONLY a JSON object with the key 'name'.",
  
  "user_prompt_template": "Translate this Chinese dish name to English:

{name}

Return format:
{
  \"name\": \"English translation\"
}"
}
```

**食谱描述翻译**

```json
{
  "system_prompt": "You are a professional food writer and translator. Translate the following Chinese recipe description into warm, inviting English that makes readers want to cook the dish. Preserve cultural context and cooking traditions. Return a JSON object with keys 'name' and 'description'.",
  
  "user_prompt_template": "Dish name: {name}

Description in Chinese:
{description}

Translate to English. Return format:
{
  \"name\": \"English dish name\",
  \"description\": \"Translated description in English\"
}"
}
```

**烹饪步骤翻译**

```json
{
  "system_prompt": "You are translating cooking instructions from Chinese to English. The instructions should be:
- Clear and action-oriented
- Use precise culinary verbs (sauté, simmer, dice, etc.)
- Maintain the original sequence and detail level

Return a JSON array of steps.",
  
  "user_prompt_template": "Translate these cooking steps to English:

{steps}

Return format:
{
  \"steps\": [
    {\"step_number\": 1, \"instruction\": \"English instruction\"},
    {\"step_number\": 2, \"instruction\": \"English instruction\"}
  ]
}"
}
```

---

### 翻译质量检查

#### 自动质量评分

```typescript
function calculateQualityScore(
  originalText: string,
  translatedText: string,
  aiProvider: string
): number {
  let score = 0.5; // 基础分
  
  // 1. 长度合理性检查（译文不应过短或过长）
  const lengthRatio = translatedText.length / originalText.length;
  if (lengthRatio >= 0.5 && lengthRatio <= 2.0) {
    score += 0.2;
  }
  
  // 2. 是否包含中文字符（不应该有）
  const containsChinese = /[\u4e00-\u9fa5]/.test(translatedText);
  if (!containsChinese) {
    score += 0.2;
  }
  
  // 3. 标点符号检查（不应有中文标点）
  const chinesePunctuation = /[，。！？；：""''【】《》（）]/.test(translatedText);
  if (!chinesePunctuation) {
    score += 0.1;
  }
  
  return Math.min(score, 1.0);
}
```

#### 人工审核界面

```plaintext
┌────────────────────────────────────────────────────────────┐
│  翻译审核: 红烧肉                                     [关闭] │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────────┐  │
│  │  原文（中文）          │  │  AI 翻译（英文）          │  │
│  │                       │  │                           │  │
│  │  菜名：红烧肉         │  │  Name: Braised Pork      │  │
│  │                       │  │                           │  │
│  │  简介：                │  │  Description:            │  │
│  │  这道红烧肉色泽红亮， │  │  This braised pork dish  │  │
│  │  肥而不腻，入口即化， │  │  features a rich red...  │  │
│  │  是一道经典的家常菜。 │  │  ...classic home-style   │  │
│  │                       │  │  recipe.                  │  │
│  └──────────────────────┘  └──────────────────────────┘  │
│                                                              │
│  AI 质量评分：⭐⭐⭐⭐☆ (0.85)                             │
│  模型：GPT-4 Turbo  |  翻译时间：2024-01-05 10:30           │
│                                                              │
│  审核意见：                                                  │
│  ● 通过（无需修改）                                         │
│  ○ 需要修改（下方编辑）                                     │
│  ○ 拒绝（重新翻译）                                         │
│                                                              │
│  修改译文（可选）：                                          │
│  ┌────────────────────────────────────────────────────┐   │
│  │ [可编辑文本框，预填充 AI 翻译内容]                  │   │
│  └────────────────────────────────────────────────────┘   │
│                                                              │
│  [✅ 保存审核结果] [🔄 重新翻译] [取消]                     │
└────────────────────────────────────────────────────────────┘
```

---

## 👤 运营者工作流示例

### 场景 1：创建新食谱（完整流程）

#### 步骤 1：进入后台，点击"新建食谱"

```plaintext
运营者 keko hu 登录后台 → 点击侧边栏"食谱管理" → 点击"+ 新建食谱"按钮
```

#### 步骤 2：填写中文内容

```plaintext
左侧输入框：
- 菜名：蒜蓉蒸排骨
- 简介：排骨软烂，蒜香浓郁，是一道经典的粤菜蒸菜，营养又美味。
- 难度：简单
- 准备时间：15 分钟
- 烹饪时间：30 分钟
- 份量：2 人份
- 菜系：粤菜
- 地区：广东
- 上传封面图
```

#### 步骤 3：添加食材

```plaintext
主料：
- 搜索"排骨" → 选择 → 输入用量"500克"
- 搜索"大蒜" → 选择 → 输入用量"10瓣"

调料：
- 搜索"生抽" → 选择 → 输入"2勺"
- 搜索"料酒" → 选择 → 输入"1勺"
- 搜索"盐" → 选择 → 输入"适量"
```

#### 步骤 4：添加烹饪步骤

```plaintext
步骤 1: 排骨洗净，冷水下锅焯水，去除血沫后捞出。
步骤 2: 大蒜剁成蒜蓉，加入生抽、料酒、盐调成酱汁。
步骤 3: 排骨放入盘中，倒入蒜蓉酱汁，拌匀腌制 10 分钟。
步骤 4: 蒸锅水开后，放入排骨，大火蒸 30 分钟即可。
```

#### 步骤 5：选择标签

```plaintext
☑ 快手菜
☑ 下饭
☑ 蒸菜
```

#### 步骤 6：翻译设置

```plaintext
翻译模式：☑ 自动翻译（发布时）
目标语言：☑ 英文
```

#### 步骤 7：点击"保存草稿"

```plaintext
系统反馈：
✅ 食谱已保存为草稿
- 食谱 ID: recipe_abc123
- 状态: 草稿
- 翻译状态: 未翻译
```

#### 步骤 8：预览翻译效果

```plaintext
运营者点击"立即翻译"按钮
系统显示：AI 正在翻译中...（⏳ 约 5 秒）

右侧预览区更新：
Name: Steamed Pork Ribs with Garlic
Description: Tender ribs with rich garlic aroma, a classic Cantonese steamed dish that's both nutritious and delicious.
Difficulty: Easy
Prep Time: 15 minutes
Cook Time: 30 minutes

✅ 翻译完成（质量评分：0.88）
```

#### 步骤 9：检查翻译质量

```plaintext
运营者查看英文预览，发现：
- 菜名翻译准确 ✅
- 简介流畅自然 ✅
- 步骤翻译清晰 ✅

决定：无需修改，直接发布
```

#### 步骤 10：点击"发布"

```plaintext
系统执行：
1. 更新食谱状态为"已发布"
2. 设置发布时间
3. 触发翻译任务队列（异步）
   - 食谱主内容翻译（已完成）
   - 步骤 1-4 翻译（进行中）
   - 食材用量翻译（排队中）
4. 生成 sitemap 条目
5. 发送发布成功通知

✅ 食谱已发布！
   - 中文版: /zh/recipes/suan-rong-zheng-pai-gu
   - 英文版: /en/recipes/steamed-pork-ribs-with-garlic
```

---

### 场景 2：编辑已发布食谱

#### 步骤 1：进入编辑页面

```plaintext
运营者在列表页点击"蒜蓉蒸排骨" → 点击"编辑"按钮
```

#### 步骤 2：修改中文内容

```plaintext
将简介修改为：
"排骨软烂入味，蒜香浓郁扑鼻，是一道经典的粤菜蒸菜，制作简单却营养丰富，特别适合家庭日常食用。"

将烹饪时间从 30 分钟改为 35 分钟
```

#### 步骤 3：点击"保存"

```plaintext
系统提示：
⚠️ 检测到内容已修改，是否重新翻译？
[立即翻译] [稍后手动翻译] [取消]

运营者选择：[立即翻译]
```

#### 步骤 4：等待翻译完成

```plaintext
系统显示：正在更新英文版本...（⏳ 约 3 秒）

右侧预览区更新：
Description: Tender ribs infused with flavor, rich with garlic aroma - a classic Cantonese steamed dish that's simple to make yet nutritious, perfect for everyday family meals.
Cook Time: 35 minutes

✅ 翻译已更新
```

#### 步骤 5：保存并查看前端

```plaintext
运营者点击"前往查看"按钮

系统打开新标签页：
- 英文版页面立即生效
- 中文版页面同步更新

✅ 修改成功！
```

---

### 场景 3：翻译失败处理

#### 情况：API 超限或服务故障

```plaintext
运营者发布食谱后，收到系统通知：
⚠️ 翻译任务失败

点击查看详情：
- 食谱名: 蒜蓉蒸排骨
- 错误: OpenAI API Rate Limit Exceeded (429)
- 重试次数: 2/3
- 下次重试: 2024-01-05 10:40:00
```

#### 运营者操作选项

**选项 1：等待自动重试**

```plaintext
系统会在 10 分钟后自动重试（共 3 次）
无需人工干预
```

**选项 2：切换到备用 AI 服务**

```plaintext
运营者进入"AI 配置管理" → 修改配置：
将 recipe_description 的服务商从 OpenAI 改为 Anthropic
点击"重新翻译"按钮

✅ 使用 Claude 3.5 成功完成翻译
```

**选项 3：手动输入英文**

```plaintext
运营者点击"手动编辑英文"按钮
直接在右侧输入框填写英文内容
点击"保存"

✅ 手动翻译已保存
```

---

## 📋 开发检查清单（Development Checklist）

### 阶段 1：数据库和核心模型（Week 1-2）

#### 数据库设计

- [ ]  创建所有主表（recipes, ingredients, tags, categories, regions 等）

- [ ]  创建所有翻译表（recipe_translations, ingredient_translations 等）

- [ ]  创建辅助表（recipe_ingredients, recipe_steps, recipe_tags 等）

- [ ]  创建系统表（translation_configs, translation_jobs, admin_users 等）

- [ ]  添加所有必要的索引

- [ ]  添加外键约束

- [ ]  添加唯一约束（防止重复翻译记录）

- [ ]  创建数据库迁移脚本

#### ORM 模型定义

- [ ]  定义所有 Prisma/TypeORM 模型

- [ ]  配置模型关系（一对多、多对多）

- [ ]  设置默认值和验证规则

- [ ]  生成类型定义文件

#### 测试数据

- [ ]  准备种子数据（seed.sql）

- [ ]  导入 50+ 个测试食谱

- [ ]  导入 200+ 个常用食材

- [ ]  导入基础标签和菜系数据

**验收标准**：

- 数据库可正常创建和迁移

- 所有表和字段符合设计文档

- 测试数据可一键导入

---

### 阶段 2：后台管理界面（Week 3-4）

#### 认证系统

- [ ]  实现管理员登录功能

- [ ]  实现 JWT 或 Session 认证

- [ ]  实现权限控制（admin/editor 角色）

- [ ]  实现密码加密存储

- [ ]  实现"记住我"功能

#### 布局和导航

- [ ]  创建后台主布局（侧边栏 + 顶栏）

- [ ]  实现侧边栏菜单（食谱管理、食材管理等）

- [ ]  实现面包屑导航

- [ ]  实现响应式设计（适配平板）

#### 食谱管理页面

- [ ]  食谱列表页：

  - [ ]  分页加载

  - [ ]  搜索和筛选功能

  - [ ]  批量操作（发布、删除、翻译）

  - [ ]  翻译状态显示

- [ ]  食谱编辑页：

  - [ ]  左右分栏布局（中文输入 + 英文预览）

  - [ ]  富文本编辑器（简介和步骤）

  - [ ]  图片上传（拖拽上传）

  - [ ]  食材自动完成搜索

  - [ ]  步骤拖拽排序

  - [ ]  标签多选

  - [ ]  实时保存草稿

#### 食材管理页面

- [ ]  食材列表页（搜索、分类筛选）

- [ ]  食材编辑弹窗（中文输入 + 英文预览）

- [ ]  食材图标上传和管理

- [ ]  批量导入 CSV 功能

#### 标签管理页面

- [ ]  标签列表（按类型分组）

- [ ]  标签编辑弹窗

- [ ]  标签颜色和图标选择器

#### 首页配置页面

- [ ]  区块列表（拖拽排序）

- [ ]  横幅编辑器

- [ ]  精选食谱选择器

- [ ]  实时预览功能

#### 图片库页面

- [ ]  网格视图和列表视图

- [ ]  图片搜索和筛选

- [ ]  批量上传

- [ ]  图片详情和使用情况统计

#### AI 配置管理页面

- [ ]  配置列表和编辑界面

- [ ]  API Key 加密存储

- [ ]  测试翻译功能

- [ ]  提示词编辑器（支持语法高亮）

#### 翻译任务管理页面

- [ ]  任务列表（实时刷新）

- [ ]  任务详情弹窗

- [ ]  重试和删除操作

- [ ]  批量操作

#### 系统设置页面

- [ ]  账号信息编辑

- [ ]  密码修改

- [ ]  API Key 管理

- [ ]  多语言开关设置

- [ ]  数据导出功能

**验收标准**：

- 所有页面可正常访问

- 所有表单可正常提交

- 图片上传功能正常

- UI 符合设计稿

---

### 阶段 3：AI 翻译集成（Week 5-6）

#### 翻译服务封装

- [ ]  创建 OpenAI 客户端封装

- [ ]  创建 Anthropic 客户端封装

- [ ]  创建 DeepL 客户端封装（备用）

- [ ]  实现统一的翻译接口

- [ ]  实现 API Key 解密函数

- [ ]  实现错误处理和重试逻辑

#### 任务队列系统

- [ ]  配置 Redis 连接

- [ ]  创建 BullMQ 队列（translation-jobs）

- [ ]  实现任务入队函数（enqueueTranslationJob）

- [ ]  实现任务处理 Worker（translationWorker）

- [ ]  实现任务优先级排序

- [ ]  实现失败重试机制

- [ ]  实现任务状态监控

#### 翻译逻辑实现

- [ ]  实现食谱翻译函数（translateRecipe）

- [ ]  实现食材翻译函数（translateIngredient）

- [ ]  实现步骤翻译函数（translateStep）

- [ ]  实现标签翻译函数（translateTag）

- [ ]  实现质量评分算法

- [ ]  实现翻译缓存机制

#### 触发机制

- [ ]  实现自动翻译（发布时触发）

- [ ]  实现手动翻译（立即执行）

- [ ]  实现批量翻译

- [ ]  实现内容修改检测（避免重复翻译）

#### 审核功能

- [ ]  创建翻译审核界面

- [ ]  实现人工修改英文内容

- [ ]  实现审核状态标记

- [ ]  实现审核历史记录

**验收标准**：

- 发布食谱时自动触发翻译

- 翻译结果正确保存到数据库

- 翻译失败时正确显示错误信息

- 任务队列可正常处理并发任务

---

### 阶段 4：用户端前端（Week 7-8）

#### 路由设计

- [ ]  实现语言检测中间件

- [ ]  配置 /en/ 和 /zh/ 路由

- [ ]  实现语言切换功能

- [ ]  配置 Cookie 存储语言偏好

#### 页面开发

- [ ]  首页（英文版）

  - [ ]  横幅区块

  - [ ]  精选食谱卡片

  - [ ]  菜系分类

  - [ ]  时令推荐

- [ ]  食谱列表页（分页、筛选、搜索）

- [ ]  食谱详情页：

  - [ ]  展示食谱信息

  - [ ]  食材清单

  - [ ]  烹饪步骤

  - [ ]  标签展示

  - [ ]  社交分享按钮

- [ ]  菜系列表页

- [ ]  菜系详情页（该菜系下的所有食谱）

- [ ]  关于我们页面

- [ ]  博客页面（可选）

#### 数据获取逻辑

- [ ]  实现食谱列表 API（支持分页和筛选）

- [ ]  实现食谱详情 API（根据语言返回翻译内容）

- [ ]  实现菜系列表 API

- [ ]  实现标签列表 API

- [ ]  实现搜索 API（支持中英文搜索）

#### SEO 优化

- [ ]  添加 hreflang 标签

- [ ]  生成 sitemap.xml（包含所有语言版本）

- [ ]  添加 robots.txt

- [ ]  配置 Open Graph 和 Twitter Card

- [ ]  实现结构化数据（[Schema.org](http://Schema.org) Recipe）

- [ ]  优化页面标题和描述

#### 性能优化

- [ ]  实现图片懒加载

- [ ]  使用 Next.js Image 组件优化图片

- [ ]  实现静态生成（SSG）或增量静态再生（ISR）

- [ ]  配置 CDN（如 Vercel、Cloudflare）

- [ ]  实现内容缓存策略

**验收标准**：

- 英文版网站可正常访问

- 中文版网站可正常访问

- 语言切换功能正常

- SEO 标签正确配置

- Google PageSpeed Insights 得分 > 90

---

### 阶段 5：测试和优化（Week 9）

#### 功能测试

- [ ]  测试完整的食谱创建流程

- [ ]  测试食谱编辑和删除

- [ ]  测试翻译触发和结果保存

- [ ]  测试翻译失败重试机制

- [ ]  测试批量操作

- [ ]  测试图片上传和展示

- [ ]  测试搜索和筛选功能

- [ ]  测试语言切换

#### 边界测试

- [ ]  测试空内容提交

- [ ]  测试超长内容（>10000 字）

- [ ]  测试特殊字符和 Emoji

- [ ]  测试并发翻译（>100 任务）

- [ ]  测试 API 限流处理

- [ ]  测试网络断开情况

#### 性能测试

- [ ]  测试数据库查询性能（N+1 问题）

- [ ]  测试翻译 API 响应时间

- [ ]  测试任务队列处理速度

- [ ]  测试前端页面加载速度

- [ ]  测试图片加载速度

#### Bug 修复

- [ ]  修复测试中发现的所有 Bug

- [ ]  优化慢查询

- [ ]  优化前端性能

- [ ]  改进错误提示信息

**验收标准**：

- 所有核心功能正常运行

- 无严重 Bug

- 性能达标

---

### 阶段 6：上线准备（Week 10）

#### 生产环境配置

- [ ]  配置生产数据库（备份和恢复策略）

- [ ]  配置生产 Redis

- [ ]  配置环境变量（.env.production）

- [ ]  配置域名和 SSL 证书

- [ ]  配置 CDN

- [ ]  配置监控和日志（Sentry、LogRocket 等）

#### 数据迁移

- [ ]  准备初始数据（食材、标签、菜系）

- [ ]  执行数据库迁移

- [ ]  验证数据完整性

#### 文档编写

- [ ]  编写运营手册（如何创建食谱、如何审核翻译）

- [ ]  编写 API 文档

- [ ]  编写部署文档

- [ ]  编写故障排查指南

#### 上线检查

- [ ]  检查所有 API 端点可访问

- [ ]  检查所有页面可正常加载

- [ ]  检查翻译功能正常

- [ ]  检查图片上传和展示正常

- [ ]  检查 SEO 配置

- [ ]  检查 Google Analytics 和 Google Search Console 配置

**验收标准**：

- 生产环境可正常访问

- 所有功能正常运行

- 监控和日志正常记录

---

## 🎯 关键技术细节补充

### 1. 如何避免 N+1 查询问题

**问题场景**：获取食谱列表时，需要同时获取每个食谱的翻译内容

**错误做法**：

```typescript
// ❌ 会产生 N+1 查询
const recipes = await db.query.recipes.findMany();

for (const recipe of recipes) {
  const translation = await db.query.recipe_translations.findFirst({
    where: and(
      eq(recipe_translations.recipe_id, recipe.id),
      eq(recipe_translations.language_code, 'en')
    ),
  });
  
  recipe.translation = translation;
}
```

**正确做法**（使用 JOIN）：

```typescript
// ✅ 一次查询搞定
const recipes = await db
  .select({
    id: recipes.id,
    name_zh: recipes.name_zh,
    translation: recipe_translations,
  })
  .from(recipes)
  .leftJoin(
    recipe_translations,
    and(
      eq(recipe_translations.recipe_id, recipes.id),
      eq(recipe_translations.language_code, 'en')
    )
  )
  .where(eq(recipes.status, 'published'));
```

---

### 2. 如何处理图片存储

**推荐方案**：使用对象存储服务（如 AWS S3、Cloudflare R2、Backblaze B2）

**上传流程**：

```typescript
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';

async function uploadImage(file: File): Promise<string> {
  const s3 = new S3Client({
    region: process.env.AWS_REGION,
    credentials: {
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY,
    },
  });
  
  const fileBuffer = await file.arrayBuffer();
  const fileName = `${Date.now()}-${file.name}`;
  
  await s3.send(new PutObjectCommand({
    Bucket: process.env.S3_BUCKET,
    Key: `recipes/${fileName}`,
    Body: Buffer.from(fileBuffer),
    ContentType: file.type,
  }));
  
  const imageUrl = `https://${process.env.CDN_DOMAIN}/recipes/${fileName}`;
  return imageUrl;
}
</string>
```

**图片压缩和优化**（使用 Next.js Image）：

```typescript
<img src="{recipe.cover_image_url}" alt="{recipe.name}" width="{800}" height="{600}" quality="{85}" placeholder="blur" blurdataurl="{recipe.blur_data_url}">
```

---

### 3. 如何实现搜索功能（中英文混合搜索）

**方案 1：PostgreSQL 全文搜索**

```sql
-- 创建全文搜索索引
CREATE INDEX idx_recipes_search_zh ON recipes 
  USING gin(to_tsvector('chinese', name_zh || ' ' || COALESCE(description_zh, '')));

CREATE INDEX idx_translations_search_en ON recipe_translations 
  USING gin(to_tsvector('english', name || ' ' || COALESCE(description, '')));
```

**查询实现**：

```typescript
async function searchRecipes(keyword: string, lang: string) {
  if (lang === 'zh') {
    return await db.query.recipes.findMany({
      where: sql`to_tsvector('chinese', ${recipes.name_zh} || ' ' || COALESCE(${recipes.description_zh}, ''))
                 @@ plainto_tsquery('chinese', ${keyword})`,
    });
  } else {
    return await db
      .select()
      .from(recipes)
      .innerJoin(
        recipe_translations,
        eq(recipe_translations.recipe_id, recipes.id)
      )
      .where(
        and(
          eq(recipe_translations.language_code, lang),
          sql`to_tsvector('english', ${recipe_translations.name} || ' ' || COALESCE(${recipe_translations.description}, ''))
              @@ plainto_tsquery('english', ${keyword})`
        )
      );
  }
}
```

**方案 2：使用 Algolia 或 Meilisearch（更强大，推荐生产环境使用）**

```typescript
import { MeiliSearch } from 'meilisearch';

const client = new MeiliSearch({
  host: process.env.MEILISEARCH_HOST,
  apiKey: process.env.MEILISEARCH_API_KEY,
});

// 索引数据
async function indexRecipe(recipe: Recipe, translation: Translation) {
  await client.index('recipes_en').addDocuments([{
    id: recipe.id,
    name: translation.name,
    description: translation.description,
    category: translation.category,
    tags: translation.tags,
  }]);
}

// 搜索
async function searchRecipes(keyword: string, lang: string) {
  const results = await client.index(`recipes_${lang}`).search(keyword, {
    limit: 20,
    attributesToHighlight: ['name', 'description'],
  });
  
  return results.hits;
}
```

---

### 4. 如何实现实时预览（WebSocket 或轮询）

**方案 A：轮询（简单但不够优雅）**

```typescript
// 前端：每 2 秒查询翻译状态
useEffect(() => {
  const interval = setInterval(async () => {
    const response = await fetch(`/api/translations/status?jobId=${jobId}`);
    const data = await response.json();
    
    if (data.status === 'completed') {
      setTranslation(data.result);
      clearInterval(interval);
    }
  }, 2000);
  
  return () => clearInterval(interval);
}, [jobId]);
```

**方案 B：Server-Sent Events（SSE，推荐）**

```typescript
// 后端 API
export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const jobId = searchParams.get('jobId');
  
  const stream = new ReadableStream({
    async start(controller) {
      const encoder = new TextEncoder();
      
      // 监听任务状态变化
      const checkStatus = async () => {
        const job = await db.query.translation_jobs.findFirst({
          where: eq(translation_jobs.id, jobId),
        });
        
        const message = `data: ${JSON.stringify(job)}

`;
        controller.enqueue(encoder.encode(message));
        
        if (job.status === 'completed' || job.status === 'failed') {
          controller.close();
        } else {
          setTimeout(checkStatus, 1000);
        }
      };
      
      await checkStatus();
    },
  });
  
  return new Response(stream, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
    },
  });
}

// 前端
const eventSource = new EventSource(`/api/translations/stream?jobId=${jobId}`);

eventSource.onmessage = (event) => {
  const job = JSON.parse(event.data);
  
  if (job.status === 'completed') {
    setTranslation(job.translation_result);
    eventSource.close();
  } else if (job.status === 'failed') {
    setError(job.error_message);
    eventSource.close();
  }
};
```

---

## 🚀 最后的提醒

### 给 AI 程序员的话

1. **严格遵循主表+翻译表模式**：所有需要多语言的内容都必须拆分到翻译表，主表只存储中文原文。

2. **所有翻译操作必须异步**：不要在主流程中同步调用 AI API，会导致响应超时。

3. **翻译结果必须可审核**：不要直接覆盖旧翻译，保留审核字段。

4. **注意 API 限流**：OpenAI 和 Anthropic 都有速率限制，必须实现重试机制。

5. **使用 TypeScript**：所有 API 调用和数据库查询都必须有类型约束，避免运行时错误。

6. **日志很重要**：翻译过程中的每一步都应该记录日志，方便排查问题。

7. **测试先行**：在实现复杂功能前，先写单元测试和集成测试。

### 给 keko hu 的话

1. **第一次运行可能需要 1-2 周**：AI 程序需要时间理解和实现整个系统，不要着急。

2. **积极测试和反馈**：每完成一个阶段，你都要亲自测试并提供反馈。

3. **准备示例数据**：最好准备 10-20 个真实食谱的中文内容，用于测试翻译效果。

4. **监控翻译质量**：前期要多花时间审核 AI 翻译结果，调整提示词模板。

5. **预算 API 成本**：假设每个食谱翻译消耗 1000 tokens，按 GPT-4 Turbo 定价约 $0.01/食谱，100 个食谱约 $1。

---

## 📞 联系支持

如果在开发过程中遇到问题，请参考：

- 本文档的"故障排查指南"章节（可后续补充）

- 技术栈的官方文档（Next.js、Prisma、OpenAI API）

- GitHub Issues（如果项目开源）

祝开发顺利！🎉